status: todo
summary: Integrate useSchema in query.vue and add toast for schema fetch errors
references:
  - file: apps/db/app/pages/query.vue
    note: |
      Call useSchema() composable (from 006) alongside existing useFetch('/api/tables').
      Pass schema data to QueryEditor via new :schema prop.
      Handle error state from useSchema with toast notification.
  - file: apps/db/app/components/query-editor.vue
    note: |
      Receives :schema prop from query.vue.
  - file: apps/db/app/components/ui/toast/
    note: |
      Check if shadcn-vue toast exists. If not, install via CLI or create minimal custom toast.
      User preference: "naked shadcn component" if non-existent.
  - file: apps/db/app/layouts/default.vue
    note: |
      May need to add ToastProvider/Toaster at layout level if using shadcn-vue toast.
  - file: apps/db/app/components/table/[schema]/[name].vue
    note: |
      Has inline custom toast implementation. Reference for toast UI pattern.
  - file: apps/db/test/components/query-editor.nuxt.spec.ts
    note: |
      Integration test: verify query.vue passes schema to editor and handles errors.
approach:
  - In query.vue: const { data: schemaData, error: schemaError } = useSchema().
  - Watch schemaError: if present, show toast "Failed to load schema".
  - Pass :schema="mappedSchema" to QueryEditor.
  - Keep existing command palette tables fetch (or reuse schema data).
  - Toast: if no shadcn toast, install shadcn-vue Toast component. Add Toaster to layout.
  - TDD: Component/integration test verifying schema prop is passed and error toast shown.
