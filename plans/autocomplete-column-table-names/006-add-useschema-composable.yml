status: todo
summary: Create useSchema() composable in app/composables/
references:
  - file: apps/db/app/pages/query.vue
    note: |
      Uses useFetch('/api/tables') pattern. useSchema() should follow same pattern.
      Query page will call useSchema() and pass result to QueryEditor.
  - file: apps/db/app/components/query-editor.vue
    note: |
      Will receive schema prop from useSchema() via query.vue.
  - file: apps/db/server/api/schema.get.ts
    note: |
      Composable fetches from this endpoint.
  - file: apps/db/test/components/query-editor.nuxt.spec.ts
    note: |
      Component test pattern: mountSuspended with props.
      Test useSchema composable via a wrapper component or directly if nuxt/test-utils supports it.
  - file: apps/db/test/e2e/utils.ts
    note: |
      E2E setup pattern with TEST_HOST env var.
approach:
  - Create app/composables/useSchema.ts with useFetch('/api/schema').
  - Return { schema, pending, error, refresh }.
  - Schema shape mapped for editor: { [schemaName]: { [tableName]: string[] } }.
  - TDD: Write component test mounting a test component that calls useSchema, mock fetch response, assert mapped shape.
