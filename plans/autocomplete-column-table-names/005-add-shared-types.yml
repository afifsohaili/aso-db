status: todo
summary: Add SchemaInfo shared type to packages/shared/
references:
  - file: packages/shared/types.d.ts
    note: |
      Auto-generated Kysely types. DO NOT EDIT THIS FILE.
      Create a new file packages/shared/types/schema.ts for app-specific shared types.
  - file: apps/db/shared/types/table.ts
    note: |
      Existing local types: TableInfo, TableRecord, FetchTableRecordsResult, RelationInfo.
      SchemaInfo should extend TableInfo with columns: string[].
  - file: apps/db/server/api/schema.get.ts
    note: |
      Will use SchemaInfo type for response shape.
  - file: apps/db/server/api/tables.get.ts
    note: |
      Uses TableInfo[]; keep backward compatible.
approach:
  - Create packages/shared/types/schema.ts with SchemaInfo { schema, name, columns: string[] }.
  - Export from packages/shared/index.ts (create if needed).
  - Import in apps/db/server/api/schema.get.ts and apps/db/server/api/tables.get.ts.
  - No test needed for pure type definition; types verified by TypeScript compiler.
