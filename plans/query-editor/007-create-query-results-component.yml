status: todo
summary: Create QueryResults Vue component for displaying multiple result sets
references:
  - file: apps/db/app/components/table-detail.vue
  - file: apps/db/app/components/pagination.vue
approach:
  - >
    Create apps/db/app/components/query-results.vue
  - >
    Props: results: QueryResult[], loading: boolean
  - >
    For each successful QueryResult, render a collapsible panel showing: the executed SQL snippet (truncated if very long); rowCount and durationMs; reuse TableDetail component for the rows/columns grid (since it already handles column headers, null formatting, booleans, and sorting). Note: TableDetail sorting emit can be ignored or handled locally if sorting per-result-set is desired. For MVP, static display via TableDetail without interactive sorting is acceptable.
  - >
    For failed results, show a red panel with the errorMessage
  - >
    If results is empty, show a placeholder message
  - >
    If loading is true, show a spinner or Executing... text
  - >
    Use existing Tailwind color conventions (bg-gray-900, border-gray-700, text-gray-300, bg-red-900/50 for errors)
  - >
    Write a Nuxt component test in apps/db/test/components/query-results.nuxt.spec.ts that passes mock QueryResult[] and asserts correct number of result panels and error styling
