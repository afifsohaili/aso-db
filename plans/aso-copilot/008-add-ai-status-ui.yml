status: done
summary: Add AI status UI to query page and finalize integration
references:
  - files:
    - apps/db/app/pages/query.vue
    - apps/db/app/components/query-editor.vue
    - apps/db/test/e2e/query-execute.post.spec.ts
approach:
  - Fetch AI config in `query.vue` with `useFetch('/api/ai/config')`.
  - Pass `aiEnabled` prop to `<QueryEditor>` component.
  - Ensure AI status dot and token/cost display are visible in the editor toolbar.
  - Handle AI errors gracefully (yellow dot on failure, gray when disabled).
  - Add E2E test in `test/e2e/query-ai-integration.spec.ts`.
  - Test: verify ghost text appears after typing pause (with mocked AI response).
  - Test: verify Tab accepts suggestion.
  - Test: verify tokens/cost display updates after suggestion.
  - Test: verify disabled state shows gray dot.
  - Use `setupE2E` helper pattern.
  - Run all existing tests to ensure no regressions.
